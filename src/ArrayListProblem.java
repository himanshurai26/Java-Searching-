public class ArrayListProblem {
    // create a program that implements a simple mobile phone with the following capabilities.
    // Able to store, modify, remove and query contact names.
    // you will want to create a separate class for contacts(name and phone number).
    // create a master class (Mobilephone) that holds the ArrayList of contacts
    // The MobliePhone class has the functionality listed above
    // Options: Quite, print list of contacts, add new contact, update existing contact, remove contact
    // and search/find contact.
    // when adding or updating be sure to check if the contact already exists
    // e.g. no ints, no .get(i) etc
    // Mobilephone should do everything with contacts only.


}
