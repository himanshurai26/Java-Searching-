import java.util.*;
import java.util.ArrayList;

public class Arrlist2 {
    // create an Arraylist which going to
    // contains a list of Number

    public static void main(String[] args) {


        ArrayList<Integer> Numbers = new ArrayList<Integer>();

        // add Number to list
        Numbers.add(23);
        Numbers.add(43);
        Numbers.add(88);
        Numbers.add(98);
        Numbers.add(32);
        Numbers.add(21);
        Numbers.add(8);
        Numbers.add(44);
        Numbers.add(76);
        Numbers.add(34);
        System.out.println("Programee is doe we can quite ");
        // there is some error in the code so we have to fixed in it so that that it
        // can run in effectiveway


        // forEach method of ArrayList and print numbers
        Numbers.forEach((print) ->System.out.println(print));

        // in java there is different way to write the java  programe to write the forEach concept in the
        // Firstly we have to write the pograme in
        // we can access the java forEach concept by the

    }
}
